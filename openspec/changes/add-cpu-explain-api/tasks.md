## 1. API
- [x] 1.1 Add CPU explain methods for default and custom bridge options.
- [x] 1.2 Map planner explain errors to `CoreError::QueryPlanning`.

## 2. Validation
- [x] 2.1 Add CPU tests for explain supported and unsupported projection status.
- [x] 2.2 Run `cargo test -p idb-executor-cpu` and `openspec validate --changes --strict --no-interactive`.
