## 1. Planner Bridge
- [x] 1.1 Compile multiple semantic predicates into one centroid vector query.
- [x] 1.2 Compile minimum semantic threshold as the strictest threshold across semantic predicates.
- [x] 1.3 Add deterministic planner tests for multi-semantic compilation.

## 2. CPU Integration
- [x] 2.1 Add CPU integration test for multi-semantic text query execution.

## 3. Validation
- [x] 3.1 Run `cargo test --workspace` and `openspec validate --changes --strict --no-interactive`.
